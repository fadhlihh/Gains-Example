using UnityEngine;
using Gains.Module.ScanData;

namespace Gains.Module.DataEntryScene
{
    public class DataEntrySceneController : MonoBehaviour
    {
        private void Awake()
        {
            // Debug.Log(ScanResultData.Instance.Result);
        }
    }
}
